# ローカルAI組織 — ナレッジベース

このリポジトリは「ローカルで動くAI営業組織」のナレッジベースです。
商談ごとの議事録・依存関係・成果物（企画・スライド）・反省点を一元管理し、
AIエージェントが横断的に参照・更新できる状態を保つことを目的とします。

## 1. ディレクトリ方針（2層構造）

このリポジトリは「ナレッジ層（PARA）」と「実行設定層（.claude/）」を分離している。

### 1-A. ナレッジ層（PARA） — 営業情報の保管

| 層 | 役割 | 内容 |
| --- | --- | --- |
| `01_Projects/` | 進行中の能動的な仕事（締切・ゴールあり） | 各商談ごとのフォルダ。トランスクリプト・議事録・成果物を内包 |
| `02_Areas/` | 継続的な責任領域（終わりなし） | 会社情報・人物・営業パイプライン・自社情報 |
| `03_Resources/` | 参考資料・テンプレート・ナレッジ | 議事録テンプレ、プレイブック、自社プロダクト情報 |
| `04_Archive/` | 完了・失注した案件 | `won-deals/`（受注） / `lost-deals/`（失注） |

### 1-B. 実行設定層（`.claude/`） — Claude Code が自動認識する

| パス | 役割 |
| --- | --- |
| `.claude/skills/<name>/SKILL.md` | スキル（手順書）。`/<name>` で呼べる |
| `.claude/agents/<name>.md` | サブエージェント（独立コンテキストの専門家） |
| `.claude/settings.local.json` | ローカル権限設定（コミット対象外） |

「ワークフロー」は公式に独立概念は無く、`skills` の本文に手順を書くか、複数 skill / agent を組み合わせて表現する。

### 1-C. その他

- `inbox/` — 未整理のトランスクリプト等を一時投入する場所

## 2. ドキュメントのフロントマター規約

すべてのMarkdownはYAMLフロントマターで関係性を表現します。
**依存関係を辿れる状態を最優先**にしてください。

```yaml
---
id: <ユニークID>            # 例: meeting-2026-04-15-techfrontier-01
type: <種類>                 # meeting | proposal | slide | company | contact | retro
date: 2026-04-15             # 主たる日付（ISO形式）
project: <プロジェクトID>     # 01_Projects/ 配下のフォルダ名
company: <会社ID>            # 02_Areas/companies/ 配下のフォルダ名
attendees: [<contact-id>, ...]
depends_on: [<id>, ...]      # 上流（この文書が前提とするもの）
relates_to: [<id>, ...]      # 横の関連
next_actions: [<id>, ...]    # 下流（この文書から派生するTODO/次会議）
status: draft | review | final
tags: [<tag>, ...]
---
```

リンクは `[[id]]` 形式で本文中に記述すると、AIが追跡しやすくなります。

## 3. 命名規則

- プロジェクトフォルダ: `YYYY-Q[1-4]_<会社略称>_<案件名>`（例: `2026-Q2_テックフロンティア_SaaS導入`）
- 議事録: `YYYY-MM-DD_<回次>_<議題>.md`（例: `2026-04-15_01_初回ヒアリング.md`）
- 会社フォルダ: 会社略称（日本語可、半角スペース不可）

## 4. ワークフロー（標準）

1. **トランスクリプト投入** → `inbox/` または `01_Projects/<案件>/transcripts/` に置く
2. **議事録生成** → `skills/transcript-to-minutes/` のスキルで `minutes/` に出力
3. **依存関係の更新** → 議事録の `next_actions` から次のアクション（提案書・次回会議）を自動派生
4. **成果物作成** → `deliverables/`（企画書・スライド原稿）
5. **クローズ** → 受注/失注時に `04_Archive/` に移動し、retro（振り返り）を作成

## 5. 失注/受注からの学習

`04_Archive/lost-deals/<案件>/retro.md` には必ず以下を残す:
- 失注理由（事実）
- 仮説（なぜそうなったか）
- 次回以降の打ち手（他案件へ反映するルール）

新規商談を開始する際は、AIが `04_Archive/lost-deals/**/retro.md` を横断し、
類似度の高い学びを `01_Projects/<新案件>/lessons.md` に転記してから提案を作ること。

## 6. 自社情報・会社情報の置き場所

- 自社情報（プロダクト・営業資料の前提）: `02_Areas/our-company/`
- 顧客企業の情報: `02_Areas/companies/<会社略称>/`
- 人物（窓口・キーマン）: `02_Areas/contacts/<会社略称>_<氏名>.md`

## 7. AIエージェントへの指示

- 議事録を作る時は **必ず** `skills/transcript-to-minutes/SKILL.md` の手順に従う
- ファイルを作成・更新したら、関連する上流ドキュメントの `next_actions` も同時に更新する
- フロントマターの `id` は重複させない（既存IDを必ず検索してから採番）
- 日付は今日（実行日）を絶対日付で書く。「明日」「来週」等の相対表現は禁止
